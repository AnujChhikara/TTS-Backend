// import { Router } from 'express'

// const router = Router()

// router.route('/:interviewId').get(interview.getInterviewById)
// router.route('/user/:interviewId').get(interview.getInterviewByUserId)
// router.post('/').post(interview.createInterview)
// router.route('/:interviewId').patch(interview.updateInterview)
// router.route('/:interviewId').delete(interview.deleteInterview)

// export default router
